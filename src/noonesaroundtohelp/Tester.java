package noonesaroundtohelp;

import java.io.*;

public class Tester {

	public static void main(String args[]) throws IOException {
		File image1 = null;
		File image2 = new File("");
		//FileReader fileReader = new FileReader(image2);
		FileInputStream fis = new FileInputStream(image1);
		BufferedInputStream copy = null;
		FileOutputStream fos = new FileOutputStream(image2);
		BufferedOutputStream paste = new BufferedOutputStream(fos);
		
		
		

	}

}
